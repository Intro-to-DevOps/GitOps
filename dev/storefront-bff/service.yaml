apiVersion: v1
kind: Service
metadata:
  name: storefront-bff
  namespace: yas-dev
  labels:
    app: storefront-bff
spec:
  type: ClusterIP
  selector:
    app: storefront-bff
  ports:
  - port: 80
    targetPort: 80
