apiVersion: v1
kind: Service
metadata:
  name: backoffice-ui
  namespace: yas-dev
  labels:
    app: backoffice-ui
spec:
  type: NodePort
  selector:
    app: backoffice-ui
  ports:
  - port: 3000
    targetPort: 3000
