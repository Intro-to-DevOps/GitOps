apiVersion: v1
kind: Service
metadata:
  name: storefront-ui
  namespace: yas-dev
  labels:
    app: storefront-ui
spec:
  type: NodePort
  selector:
    app: storefront-ui
  ports:
  - port: 3000
    targetPort: 3000
